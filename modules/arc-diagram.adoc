/**
 * Creates and returns an Arc Diagram Chart.
 * @category Charts
 * @example anychart.arcDiagram
 * @param {?(anychart.data.View|anychart.data.Set|Array|string)=} opt_data Arc diagram chart data.
 * @param {(anychart.enums.TextParsingMode|anychart.data.TextParsingSettings)=} opt_csvSettings If CSV string is passed, you can pass CSV parser settings here as a hash map.
 * @return {anychart.charts.ArcDiagram} Arc diagram chart.
 * @since 9.0.0
 */
anychart.arcDiagram;
