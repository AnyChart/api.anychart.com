/**
 * Creates and returns a Waffle Chart.
 * @category Charts
 * @listing See listing
 * var chart = anychart.waffle([
 *   {x: 'Product A', value: 45},
 *   {x: 'Product B', value: 30},
 *   {x: 'Product C', value: 25}
 * ]);
 * @param {?(anychart.data.View|anychart.data.Set|Array|string)=} opt_data Waffle chart data.
 * @param {(anychart.enums.TextParsingMode|anychart.data.TextParsingSettings)=} opt_csvSettings If CSV string is passed, you can pass CSV parser settings here as a hash map.
 * @return {anychart.charts.Waffle} Waffle chart.
 * @since 9.0.0
 */
anychart.waffle;
