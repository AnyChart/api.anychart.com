/**
 * Creates and returns a Column Chart.
 * Each passed parameter is considered as a set of data which is to become a base for the Column Series.
 * @category Charts
 * @example anychart.column
 * @param {...(anychart.data.View|anychart.data.Set|Array)} var_args Column chart data.
 * @return {anychart.charts.Cartesian} Chart with defaults for column series.
 */
anychart.column;

/**
 * Creates and returns a HiLo Chart
 * @category Charts
 * @example anychart.hilo
 * @param {...(anychart.data.View|anychart.data.Set|Array)} var_args HiLo chart data.
 * @return {anychart.charts.Cartesian} Chart with defaults for HiLo series.
 * @since 7.14.0
 */
anychart.hilo;

/**
 * Creates and returns a Dumbbell Chart.
 * Each passed parameter is considered as a set of data which is to become a base for the Dumbbell Series.
 * @category Charts
 * @listing See listing
 * var chart = anychart.dumbbell([
 *   {x: 'A', low: 10, high: 40},
 *   {x: 'B', low: 20, high: 55}
 * ]);
 * @param {...(anychart.data.View|anychart.data.Set|Array)} var_args Dumbbell chart data.
 * @return {anychart.charts.Cartesian} Chart with defaults for dumbbell series.
 * @since 9.0.0
 */
anychart.dumbbell;

/**
 * Creates and returns a Lollipop Chart.
 * Each passed parameter is considered as a set of data which is to become a base for the Lollipop Series.
 * @category Charts
 * @listing See listing
 * var chart = anychart.lollipop([
 *   {x: 'A', value: 40},
 *   {x: 'B', value: 55}
 * ]);
 * @param {...(anychart.data.View|anychart.data.Set|Array)} var_args Lollipop chart data.
 * @return {anychart.charts.Cartesian} Chart with defaults for lollipop series.
 * @since 9.0.0
 */
anychart.lollipop;

