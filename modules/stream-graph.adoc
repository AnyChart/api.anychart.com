/**
 * Creates and returns a Stream Graph Chart.<br/>
 * Each passed parameter is considered as a set of data which is to become a base for a Spline Area series.
 * @category Charts
 * @example anychart.streamGraph
 * @param {...(anychart.data.View|anychart.data.Set|Array)} var_args Stream graph data.
 * @return {anychart.charts.StreamGraph} Stream graph chart with defaults.
 * @since 9.0.0
 */
anychart.streamGraph;
