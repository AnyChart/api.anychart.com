/**
 * Creates and returns an OHLC Chart.
 * Each passed parameter is considered as a set of data which is to become a base for the OHLC Series.
 * @category Charts
 * @example anychart.ohlc
 * @param {...(anychart.data.View|anychart.data.Set|Array)} var_args OHLC chart data.
 * @return {anychart.charts.Cartesian} Chart with defaults for OHLC series.
 * @since 8.4.2
 */
anychart.ohlc;