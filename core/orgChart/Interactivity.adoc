/**
 * Interactivity class for org charts.<br/>
 * <b>Note:</b> Use {@link anychart.charts.OrgChart#interactivity} method to get this class instance.<br/>
 * @param {anychart.charts.OrgChart} parent Chart instance.
 * @constructor
 * @extends {anychart.core.utils.Interactivity}
 * @since 9.0.0
 */
anychart.core.orgChart.Interactivity;


//----------------------------------------------------------------------------------------------------------------------
//
//  anychart.core.orgChart.Interactivity.prototype.drag
//
//----------------------------------------------------------------------------------------------------------------------

/**
 * Getter for the drag state.
 * @shortDescription Drag settings.
 * @category Interactivity
 * @detailed Dragging pans the tree inside the chart area.
 * <ul>
 *   <li><b>Default:</b> true.</li>
 * </ul>
 * @listing See listing
 * var dragEnabled = chart.interactivity().drag();
 * @example anychart.core.orgChart.Interactivity.drag_get
 * @return {boolean} The drag state.
 * @since 9.0.0
 */
anychart.core.orgChart.Interactivity.prototype.drag;

/**
 * Setter for the drag state.
 * @detailed <ul>
 *   <li><b>true:</b> the tree can be panned with the mouse or with a touch gesture.</li>
 *   <li><b>false:</b> the tree stays where the layout put it.</li>
 * </ul>
 * See {@link anychart.core.orgChart.Interactivity#boundsMode} for the way a pan is limited.
 * @listing See listing
 * chart.interactivity().drag(false);
 * @example anychart.core.orgChart.Interactivity.drag_set
 * @param {boolean} value [true] The drag state.
 * @return {anychart.core.orgChart.Interactivity} Self instance for method chaining.
 * @since 9.0.0
 */
anychart.core.orgChart.Interactivity.prototype.drag;

//----------------------------------------------------------------------------------------------------------------------
//
//  anychart.core.orgChart.Interactivity.prototype.zoomOnMouseWheel
//
//----------------------------------------------------------------------------------------------------------------------

/**
 * Getter for the mouse wheel zoom state.
 * @shortDescription Mouse wheel zoom settings.
 * @category Interactivity
 * @detailed <ul>
 *   <li><b>Default:</b> true.</li>
 * </ul>
 * @listing See listing
 * var zoomOnMouseWheel = chart.interactivity().zoomOnMouseWheel();
 * @example anychart.core.orgChart.Interactivity.zoomOnMouseWheel_get
 * @return {boolean} The mouse wheel zoom state.
 * @since 9.0.0
 */
anychart.core.orgChart.Interactivity.prototype.zoomOnMouseWheel;

/**
 * Setter for the mouse wheel zoom state.
 * @detailed <ul>
 *   <li><b>true:</b> the mouse wheel zooms the tree, and the mouse wheel scroll is turned off.</li>
 *   <li><b>false:</b> the mouse wheel does not zoom the tree, and the mouse wheel scroll keeps its current state.</li>
 *   <li><b>Limits:</b> a wheel zoom stops at the minimum and the maximum zoom levels of the chart.</li>
 * </ul>
 * See {@link anychart.core.orgChart.Interactivity#scrollOnMouseWheel} for the other mouse wheel option.
 * @listing See listing
 * chart.interactivity().zoomOnMouseWheel(false);
 * @example anychart.core.orgChart.Interactivity.zoomAndScroll_set
 * @param {boolean} value [true] The mouse wheel zoom state.
 * @return {anychart.core.orgChart.Interactivity} Self instance for method chaining.
 * @since 9.0.0
 */
anychart.core.orgChart.Interactivity.prototype.zoomOnMouseWheel;

//----------------------------------------------------------------------------------------------------------------------
//
//  anychart.core.orgChart.Interactivity.prototype.scrollOnMouseWheel
//
//----------------------------------------------------------------------------------------------------------------------

/**
 * Getter for the mouse wheel scroll state.
 * @shortDescription Mouse wheel scroll settings.
 * @category Interactivity
 * @detailed <ul>
 *   <li><b>Default:</b> false.</li>
 * </ul>
 * @listing See listing
 * var scrollOnMouseWheel = chart.interactivity().scrollOnMouseWheel();
 * @example anychart.core.orgChart.Interactivity.scrollOnMouseWheel_get
 * @return {boolean} The mouse wheel scroll state.
 * @since 9.0.0
 */
anychart.core.orgChart.Interactivity.prototype.scrollOnMouseWheel;

/**
 * Setter for the mouse wheel scroll state.
 * @detailed <ul>
 *   <li><b>true:</b> the mouse wheel scrolls the tree, and the mouse wheel zoom is turned off.</li>
 *   <li><b>false:</b> the mouse wheel does not scroll the tree, and the mouse wheel zoom keeps its current state.</li>
 * </ul>
 * See {@link anychart.core.orgChart.Interactivity#zoomOnMouseWheel} for the other mouse wheel option.
 * @listing See listing
 * chart.interactivity().scrollOnMouseWheel(true);
 * @example anychart.core.orgChart.Interactivity.zoomAndScroll_set
 * @param {boolean} value [false] The mouse wheel scroll state.
 * @return {anychart.core.orgChart.Interactivity} Self instance for method chaining.
 * @since 9.0.0
 */
anychart.core.orgChart.Interactivity.prototype.scrollOnMouseWheel;

//----------------------------------------------------------------------------------------------------------------------
//
//  anychart.core.orgChart.Interactivity.prototype.boundsMode
//
//----------------------------------------------------------------------------------------------------------------------

/**
 * Getter for the bounds mode.
 * @shortDescription Bounds mode settings.
 * @category Interactivity
 * @detailed The bounds mode defines how far the tree can be moved out of the chart area.
 * <ul>
 *   <li><b>Default:</b> 'contain'.</li>
 * </ul>
 * @listing See listing
 * var boundsMode = chart.interactivity().boundsMode();
 * @example anychart.core.orgChart.Interactivity.boundsMode_get
 * @return {(anychart.enums.BoundsMode|string)} The bounds mode.
 * @since 9.0.0
 */
anychart.core.orgChart.Interactivity.prototype.boundsMode;

/**
 * Setter for the bounds mode.
 * @detailed <ul>
 *   <li><b>'free':</b> the tree can be panned without any limit.</li>
 *   <li><b>'contain':</b> the tree is kept inside the chart area on every step of a gesture; when the tree is bigger than the area, its far edge stops at the edge of the area.</li>
 *   <li><b>'auto-center':</b> a gesture is not limited while it runs; when a gesture ends with every node box fully out of the chart area, the tree animates back to the center in about 300 ms, and the zoom level does not change.</li>
 *   <li><b>Ignored:</b> any other value; such a call writes a warning to the console and keeps the current mode.</li>
 * </ul>
 * @listing See listing
 * chart.interactivity().boundsMode('auto-center');
 * @example anychart.core.orgChart.Interactivity.boundsMode_set
 * @param {(anychart.enums.BoundsMode|string)} value ['contain'] The bounds mode.
 * @return {anychart.core.orgChart.Interactivity} Self instance for method chaining.
 * @since 9.0.0
 */
anychart.core.orgChart.Interactivity.prototype.boundsMode;
