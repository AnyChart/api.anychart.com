/**
 * Point representing waffle chart point.
 * @param {anychart.charts.Waffle} chart Waffle chart.
 * @param {number} index Point index in chart.
 * @constructor
 * @extends {anychart.core.Point}
 * @since 9.0.0
 */
anychart.core.WafflePoint;

//----------------------------------------------------------------------------------------------------------------------
//
//  anychart.core.WafflePoint.prototype.getName
//
//----------------------------------------------------------------------------------------------------------------------

/**
 * Gets the name of the point category.
 * @shortDescription Point category name.
 * @category Data
 * @listing See listing
 * var point = chart.getPoint(1);
 * var name = point.getName();
 * @return {string} The category name.
 * @since 9.0.0
 */
anychart.core.WafflePoint.prototype.getName;

//----------------------------------------------------------------------------------------------------------------------
//
//  anychart.core.WafflePoint.prototype.getValue
//
//----------------------------------------------------------------------------------------------------------------------

/**
 * Gets the value of the point category.
 * @shortDescription Point category value.
 * @category Data
 * @listing See listing
 * var point = chart.getPoint(1);
 * var value = point.getValue();
 * @return {number} The category value.
 * @since 9.0.0
 */
anychart.core.WafflePoint.prototype.getValue;

//----------------------------------------------------------------------------------------------------------------------
//
//  anychart.core.WafflePoint.prototype.getPercent
//
//----------------------------------------------------------------------------------------------------------------------

/**
 * Gets the share of the point category in the total in percent.
 * @shortDescription Point category percent.
 * @category Data
 * @listing See listing
 * var point = chart.getPoint(1);
 * var percent = point.getPercent();
 * @return {number} The category share in percent.
 * @since 9.0.0
 */
anychart.core.WafflePoint.prototype.getPercent;
